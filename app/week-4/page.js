// pages/index.js or pages/page.js (depending on the file structure)
import NewItem from './new-item.js';

const Page = () => {
  return (
    <div>
      <h1>Add New Item Page! (the button page)</h1>
      <NewItem />
    </div>
  );
};

export default Page;
